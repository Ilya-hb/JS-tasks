1. Базовые задачи
Сумма элементов массива
Напиши функцию, которая принимает массив чисел и возвращает их сумму.

javascript
Copy
const arr = [1, 2, 3, 4, 5];
console.log(sumArray(arr)); // 15
Поиск максимального элемента
Напиши функцию, которая находит максимальный элемент в массиве.

javascript
Copy
const arr = [10, 20, 5, 30, 15];
console.log(findMax(arr)); // 30
Фильтрация массива
Напиши функцию, которая возвращает новый массив, содержащий только четные числа.

javascript
Copy
const arr = [1, 2, 3, 4, 5, 6];
console.log(filterEven(arr)); // [2, 4, 6]
Поиск элемента в массиве
Напиши функцию, которая проверяет, есть ли элемент в массиве.

javascript
Copy
const arr = [1, 2, 3, 4, 5];
console.log(contains(arr, 3)); // true
console.log(contains(arr, 6)); // false
2. Задачи средней сложности
Удаление дубликатов
Напиши функцию, которая удаляет дубликаты из массива.

javascript
Copy
const arr = [1, 2, 2, 3, 4, 4, 5];
console.log(removeDuplicates(arr)); // [1, 2, 3, 4, 5]
Сортировка массива
Напиши функцию, которая сортирует массив чисел по возрастанию.

javascript
Copy
const arr = [3, 1, 4, 1, 5, 9, 2, 6, 5];
console.log(sortArray(arr)); // [1, 1, 2, 3, 4, 5, 5, 6, 9]
Объединение массивов
Напиши функцию, которая объединяет два массива в один.

javascript
Copy
const arr1 = [1, 2, 3];
const arr2 = [4, 5, 6];
console.log(mergeArrays(arr1, arr2)); // [1, 2, 3, 4, 5, 6]
Подсчет элементов
Напиши функцию, которая подсчитывает, сколько раз каждый элемент встречается в массиве.

javascript
Copy
const arr = ['apple', 'banana', 'apple', 'orange', 'banana', 'banana'];
console.log(countElements(arr));
// { apple: 2, banana: 3, orange: 1 }
3. Задачи повышенной сложности
Поиск пересечения массивов
Напиши функцию, которая возвращает массив элементов, которые есть в обоих массивах.

javascript
Copy
const arr1 = [1, 2, 3, 4];
const arr2 = [3, 4, 5, 6];
console.log(findIntersection(arr1, arr2)); // [3, 4]
Разворот массива
Напиши функцию, которая разворачивает массив без использования метода reverse.

javascript
Copy
const arr = [1, 2, 3, 4, 5];
console.log(reverseArray(arr)); // [5, 4, 3, 2, 1]
Сумма двух чисел
Напиши функцию, которая принимает массив чисел и целевое число. Функция должна вернуть индексы двух чисел, сумма которых равна целевому числу.

javascript
Copy
const arr = [2, 7, 11, 15];
const target = 9;
console.log(twoSum(arr, target)); // [0, 1]
Слияние отсортированных массивов
Напиши функцию, которая объединяет два отсортированных массива в один отсортированный массив.

javascript
Copy
const arr1 = [1, 3, 5];
const arr2 = [2, 4, 6];
console.log(mergeSortedArrays(arr1, arr2)); // [1, 2, 3, 4, 5, 6]
4. Задачи на алгоритмы
Бинарный поиск
Напиши функцию, которая выполняет бинарный поиск элемента в отсортированном массиве.

javascript
Copy
const arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];
console.log(binarySearch(arr, 5)); // 4 (индекс элемента)
Сортировка пузырьком
Реализуй алгоритм сортировки пузырьком.

javascript
Copy
const arr = [3, 1, 4, 1, 5, 9, 2, 6, 5];
console.log(bubbleSort(arr)); // [1, 1, 2, 3, 4, 5, 5, 6, 9]
Поиск уникального элемента
Напиши функцию, которая находит уникальный элемент в массиве, где все элементы повторяются дважды, кроме одного.

javascript
Copy
const arr = [2, 3, 2, 4, 3, 5, 4];
console.log(findUnique(arr)); // 5